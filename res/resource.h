#pragma once

// Resource ids. Kept in one place because the .rc scripts and the window code that calls
// LoadIcon have to agree on the number, and nothing else will tell you when they stop.

#define IDI_ZIRCON 101
